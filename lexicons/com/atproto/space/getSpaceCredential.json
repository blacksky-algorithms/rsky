{
  "lexicon": 1,
  "id": "com.atproto.space.getSpaceCredential",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Exchange a delegation token for a space credential. The delegation token is presented as the request's Bearer credential, with a DPoP proof beside it whose key the minted credential is bound to. A client attestation is also required when the space gates on client app identity. Implemented by a space host.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["space"],
          "properties": {
            "space": {
              "type": "string",
              "description": "Permissioned space URI, in the form at://{authority}/space/{spaceType}/{skey}. Not a standard at-uri."
            },
            "clientAttestation": {
              "type": "string",
              "description": "Client attestation (JWT) signed by the application's own client authentication key. Required only when the space gates on client app identity."
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["credential"],
          "properties": {
            "credential": {
              "type": "string",
              "description": "Space credential (JWT) signed by the space authority's signing key."
            }
          }
        }
      },
      "errors": [
        {
          "name": "AttestationRequired",
          "description": "The space gates on client app identity and no client attestation was provided."
        },
        {
          "name": "NotAuthorized",
          "description": "The authority declined to authorize the requesting user."
        },
        {
          "name": "ClientNotAuthorized",
          "description": "The authority declined to authorize the requesting client application."
        },
        {
          "name": "InvalidDpopProof",
          "description": "The request carried no valid DPoP proof to bind the credential to."
        }
      ]
    }
  }
}
